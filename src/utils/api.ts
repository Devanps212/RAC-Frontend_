const configKeys  = {
    API_URL:import.meta.env.VITE_BACKEND_SERVER, //change to local host to run in local machine port 5000
    SOCKET_PORT: import.meta.env.VITE_SCOKET
}
export default configKeys